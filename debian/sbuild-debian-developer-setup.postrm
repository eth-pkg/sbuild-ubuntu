#!/bin/sh

set -e

case "$1" in
	purge)
		if [ -e /var/log/sbuild-update-all.log ]; then
			rm /var/log/sbuild-update-all.log
		fi
		;;
	*)
		echo "postrm called with unknown argument \`$1'" >&2
		exit 0
		;;
esac

#DEBHELPER#
