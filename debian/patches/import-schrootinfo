--- a/bin/sbuild-destroychroot
+++ b/bin/sbuild-destroychroot
@@ -71,6 +71,7 @@ use POSIX;
 use Getopt::Long qw(:config no_ignore_case auto_abbrev gnu_getopt);
 use Sbuild qw(usage_error);
 use Sbuild::Utility;
+use Sbuild::ChrootInfoSchroot;
 
 my $conf = Sbuild::Conf::new();
 Conf::setup($conf);
